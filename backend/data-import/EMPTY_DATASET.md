# Empty initial dataset

This release intentionally contains no imported application records.

The D1 schema and Public API are installed without seed content. Real records can be imported later by placing the approved JSON exports in `backend/data-import/input/` and regenerating `out/import.sql`.

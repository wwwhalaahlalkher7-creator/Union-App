name: leo_association
description: TRINEX — Engineering, Architecture & Technology student platform.
publish_to: "none"
version: 2.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^10.3.4
  flutter_localizations:
    sdk: flutter
  go_router: ^16.0.0
  url_launcher: ^6.3.2
  file_picker: ^8.3.7
  record: ^6.1.1
  audioplayers: ^6.5.0
  path_provider: ^2.1.5
  crypto: ^3.0.6
  convert: ^3.1.2
  llama_flutter_android: ^0.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
